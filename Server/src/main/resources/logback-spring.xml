<?xml version="1.0" encoding="UTF-8" ?>
<configuration>

    <!-- 从 application.yml 中读取日志路径和日志级别 -->
    <springProperty scope="context" name="logging.path" source="logging.file.path" defaultValue="./logs"/>
    <springProperty scope="context" name="logging.level.root" source="logging.level.root" defaultValue="INFO"/>

    <contextName>CreativeWorkshop</contextName>

    <property name="LOG_LEVEL" value="${logging.level.root}"/>
    <property name="LOG_PATH" value="${logging.path}"/>

    <!-- ==================== 控制台输出（彩色，全部级别） ==================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%yellow(%d{yyyy-MM-dd HH:mm:ss}) %red([%thread]) %highlight(%-5level) %cyan(%logger{50}) - %magenta(%msg) %n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== 控制台输出（仅 ERROR） ==================== -->
    <appender name="CONSOLE_ERROR" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%yellow(%d{yyyy-MM-dd HH:mm:ss}) %red([%thread]) %highlight(%-5level) %cyan(%logger{50}) - %magenta(%msg) %n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== 开发环境：仅控制台输出，不创建任何日志文件 ==================== -->
    <springProfile name="dev">
        <logger name="com.libiao.creative_workshop" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <root level="${LOG_LEVEL}">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ==================== 生产环境：文件 appender 仅在 prod 下初始化 ==================== -->
    <springProfile name="prod">

        <!-- 全量日志文件（按天+大小滚动） -->
        <appender name="APPLICATION" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/application.txt</file>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/%d{yyyy-MM-dd}-%i.txt</fileNamePattern>
                <maxHistory>90</maxHistory>
                <maxFileSize>200MB</maxFileSize>
                <totalSizeCap>20GB</totalSizeCap>
            </rollingPolicy>
        </appender>

        <!-- 异步写入全量日志 -->
        <appender name="APPLICATION_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
            <discardingThreshold>0</discardingThreshold>
            <queueSize>512</queueSize>
            <includeCallerData>true</includeCallerData>
            <appender-ref ref="APPLICATION"/>
        </appender>

        <!-- 错误日志文件（仅 ERROR 级别） -->
        <appender name="ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/error.txt</file>
            <filter class="ch.qos.logback.classic.filter.LevelFilter">
                <level>ERROR</level>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/error-%d{yyyy-MM-dd}-%i.txt</fileNamePattern>
                <maxHistory>90</maxHistory>
                <maxFileSize>200MB</maxFileSize>
                <totalSizeCap>10GB</totalSizeCap>
            </rollingPolicy>
        </appender>

        <!-- 异步写入错误日志 -->
        <appender name="ERROR_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
            <discardingThreshold>0</discardingThreshold>
            <queueSize>512</queueSize>
            <includeCallerData>true</includeCallerData>
            <appender-ref ref="ERROR"/>
        </appender>

        <logger name="com.libiao.creative_workshop" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="CONSOLE_ERROR"/>
            <appender-ref ref="APPLICATION_ASYNC"/>
            <appender-ref ref="ERROR_ASYNC"/>
        </logger>
        <root level="${LOG_LEVEL}">
            <appender-ref ref="CONSOLE_ERROR"/>
            <appender-ref ref="APPLICATION_ASYNC"/>
            <appender-ref ref="ERROR_ASYNC"/>
        </root>

    </springProfile>

</configuration>